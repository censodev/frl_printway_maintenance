apply plugin: "war"

bootWar {
    mainClassName = "com.goofinity.pgc_gateway.PgcGatewayApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
