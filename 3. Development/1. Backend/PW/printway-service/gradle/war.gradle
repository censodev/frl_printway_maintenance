apply plugin: "war"

bootWar {
    mainClassName = "com.goofinity.pgc_service.PgcServiceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
